; =====================================================================
;  Pengaturan PHP per-aplikasi (dibaca otomatis oleh PHP-FPM/CGI di cPanel).
;
;  PENTING untuk multi-aplikasi dalam SATU domain:
;  Beri nama sesi yang UNIK per aplikasi supaya login/sesi satu aplikasi
;  tidak menimpa aplikasi lain (mis. login absensi vs aplikasi tetangga).
;
;  Saat menyalin folder ini untuk aplikasi baru, GANTI nilai di bawah
;  menjadi nama unik aplikasi tersebut (mis. "SIMPEGSID", dst.).
;
;  Catatan: di XAMPP lokal (mod_php) file ini diabaikan — tidak masalah,
;  karena bentrok sesi hanya terjadi saat banyak aplikasi satu domain.
; =====================================================================

session.name = "SATSETSID"
